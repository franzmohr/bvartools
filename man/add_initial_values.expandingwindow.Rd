% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_initial_values.expandingwindow.R
\name{add_initial_values.expandingwindow}
\alias{add_initial_values.expandingwindow}
\title{Add Initial Values of an MCMC Chain}
\usage{
\method{add_initial_values}{expandingwindow}(object, ...)
}
\arguments{
\item{object}{a list of class 'expandingwindow', usually, the output of a call to
\code{\link{use_expanding_window}}.}

\item{...}{further arguments passed to or from other methods.}
}
\value{
An object of class 'expandingwindow'.
}
\description{
Adds initial values to a list of models by passing each element to
the respective method.
}
\examples{

data("us_macrodata")

# Starting period of the forecasting exercise
start_period <- 2007

# AR(1) models as benchmark
model <- create_bvarmodel(data = us_macrodata, p = 1,
                          deterministic = "none",
                          error = "gamma",
                          iterations = 10, burnin = 10)
# Chosen number of iterations and burn-in draws should be much higher.

# Obtain objects for expanding window estimation
model <- use_expanding_window(model, start = start_period)

# Add priors
model <- add_priors(model,
                    coef = list(v_i = 1, v_i_det = 1 / 10),
                    sigma = list(shape = 3, rate = 1))

# Add initial values
model <- add_initial_values(model)

}
