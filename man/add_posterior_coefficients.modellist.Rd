% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_posterior_coefficients.modellist.R
\name{add_posterior_coefficients.modellist}
\alias{add_posterior_coefficients.modellist}
\title{Posterior Simulation of Model Coefficients}
\usage{
\method{add_posterior_coefficients}{modellist}(object, ...)
}
\arguments{
\item{object}{an object of class 'modellist'.}

\item{...}{further arguments passed to or from other methods.}
}
\value{
A list of class 'modellist'.
}
\description{
Forwards model input to posterior simulation functions.
}
\examples{

# Load data 
data("e1")
e1 <- diff(log(e1)) * 100

# Generate model
model <- create_bvarmodel(e1, p = 1:2, deterministic = 2,
                          iterations = 20, burnin = 10)
# Chosen number of iterations and burn-in should be much higher.

# Add priors
model <- add_priors(model,
                    coef = list(v_i = 1, v_i_det = 1 / 10),
                    sigma = list(df = "k", scale = 1))

# Add initial values
model <- add_initial_values(model)

# Obtain posterior draws
model <- add_posterior_coefficients(model)

}
