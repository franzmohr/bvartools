% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_posterior_forecasts.bvarmodel.R
\name{add_posterior_forecasts.bvarmodel}
\alias{add_posterior_forecasts.bvarmodel}
\title{Add Forecasts}
\usage{
\method{add_posterior_forecasts}{bvarmodel}(object, ...)
}
\arguments{
\item{object}{an object of class 'bvarmodel', usually, the result of a call
to \code{\link{add_posterior_coefficients}} and \code{\link{add_forecast_input}}.}

\item{...}{arguments passed forward to method.}
}
\value{
The object in \code{object} with \code{posterior$forecast} added, a
\code{\link[coda]{mcmc}} object with one row per draw and \eqn{Kh} columns, stacked by
period: the \eqn{K} variables of the first forecast period, then those of the second,
and so on. \code{\link[=predict.bvarmodel]{predict}} summarises them.
}
\description{
Calculates and adds forecasts to an object of class 'bvarmodel'.
}
\examples{

# Load data
data("e1")
e1 <- diff(log(e1)) * 100

# Create model
model <- create_bvarmodel(e1, p = 2, deterministic = "const",
                          iterations = 20, burnin = 10)
# Number of iterations and burnin should be much higher.

# Add priors
model <- add_priors(model,
                    coef = list(v_i = 1, v_i_det = 1 / 10),
                    sigma = list(df = "k", scale = 1))

# Add initial values
model <- add_initial_values(model)

# Obtain posterior draws 
model <- add_posterior_coefficients(model)

# Add data used for forecast calculation
model <- add_forecast_input(model, n_ahead = 4)

# Add forecasts
model <- add_posterior_forecasts(model)


}
\seealso{
Other posterior simulation:
\code{\link[=add_forecast_input.bvarmodel]{add_forecast_input.bvarmodel()}},
\code{\link[=add_forecast_input.bvecmodel]{add_forecast_input.bvecmodel()}},
\code{\link[=add_posterior_coefficients.bvarmodel]{add_posterior_coefficients.bvarmodel()}},
\code{\link[=add_posterior_coefficients.bvecmodel]{add_posterior_coefficients.bvecmodel()}},
\code{\link[=add_posterior_forecasts.bvecmodel]{add_posterior_forecasts.bvecmodel()}},
\code{\link[=add_posterior_loglik.bvarmodel]{add_posterior_loglik.bvarmodel()}},
\code{\link[=add_posterior_loglik.bvecmodel]{add_posterior_loglik.bvecmodel()}},
\code{\link[=bvar]{bvar()}},
\code{\link[=bvec]{bvec()}},
\code{\link[=predict.bvecmodel]{predict.bvecmodel()}}
}
\concept{posterior simulation}
