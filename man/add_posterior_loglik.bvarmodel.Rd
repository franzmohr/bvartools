% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_posterior_loglik.bvarmodel.R
\name{add_posterior_loglik.bvarmodel}
\alias{add_posterior_loglik.bvarmodel}
\title{Add Log-Likelihood}
\usage{
\method{add_posterior_loglik}{bvarmodel}(object, ...)
}
\arguments{
\item{object}{an object of class 'bvarmodel', usually, the result of a call to
\code{\link{add_posterior_coefficients}}.}

\item{...}{additional arguments.}
}
\value{
The object in \code{object} with \code{posterior$loglik} added, a
\code{\link[coda]{mcmc}} object with one row per draw and one column per period of the
training sample, holding the pointwise log-likelihood that
\code{\link{selection_criteria}} uses.
}
\description{
Calculates and adds posterior log-likelihoods to an object of class 'bvarmodel'.
}
\examples{

# Load data
data("e1")
e1 <- diff(log(e1)) * 100

# Create model
model <- create_bvarmodel(e1, p = 2, deterministic = "const",
                          iterations = 20, burnin = 10)
# Number of iterations and burnin should be much higher.

# Add priors
model <- add_priors(model,
                    coef = list(v_i = 1, v_i_det = 1 / 10),
                    sigma = list(df = "k", scale = 1))

# Add initial values
model <- add_initial_values(model)

# Obtain posterior draws 
object <- add_posterior_coefficients(model)

# Add log-likelihoods
object <- add_posterior_loglik(object)


}
\seealso{
\code{\link{bvartools_model}} describes the object this returns, element by element.

Other posterior simulation:
\code{\link[=add_forecast_input.bvarmodel]{add_forecast_input.bvarmodel()}},
\code{\link[=add_forecast_input.bvecmodel]{add_forecast_input.bvecmodel()}},
\code{\link[=add_posterior_coefficients.bvarmodel]{add_posterior_coefficients.bvarmodel()}},
\code{\link[=add_posterior_coefficients.bvecmodel]{add_posterior_coefficients.bvecmodel()}},
\code{\link[=add_posterior_forecasts.bvarmodel]{add_posterior_forecasts.bvarmodel()}},
\code{\link[=add_posterior_forecasts.bvecmodel]{add_posterior_forecasts.bvecmodel()}},
\code{\link[=add_posterior_loglik.bvecmodel]{add_posterior_loglik.bvecmodel()}},
\code{\link[=add_seed]{add_seed()}},
\code{\link[=bayests_files]{bayests_files()}},
\code{\link[=bayests_posterior]{bayests_posterior()}},
\code{\link[=bvar]{bvar()}},
\code{\link[=bvec]{bvec()}},
\code{\link[=predict.bvecmodel]{predict.bvecmodel()}}
}
\concept{posterior simulation}
