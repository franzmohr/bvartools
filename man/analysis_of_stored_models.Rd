% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irf.bvarfile.R
\name{analysis_of_stored_models}
\alias{analysis_of_stored_models}
\alias{irf.bvarfile}
\alias{fevd.bvarfile}
\title{Impulse Responses and Variance Decompositions of a Stored Model}
\usage{
\method{irf}{bvarfile}(x, ..., chunk = 100)

\method{fevd}{bvarfile}(x, ..., chunk = 100)
}
\arguments{
\item{x}{an object of class 'bvarfile', from \code{\link{open_model}}.}

\item{...}{arguments of \code{\link{irf.bvarmodel}} or
\code{\link{fevd.bvarmodel}}, which do the work on each piece.}

\item{chunk}{how many draws are read at a time. Defaults to 100.}
}
\value{
What \code{\link{irf.bvarmodel}} and \code{\link{fevd.bvarmodel}}
return: an object of class 'bvarirf' or 'bvarfevd'.
}
\description{
Impulse response functions and forecast error variance decompositions of a
model in an HDF5 file, computed a piece of the chain at a time.
}
\details{
Both quantities are sums over the draws: an impulse response is a set of
quantiles of the responses of the draws, a variance decomposition their mean.
Neither needs the draws together, so the chain is read in pieces of
\code{chunk}, each piece contributes what it has, and the pieces are put
together at the end. What comes out is what the same call on the model in
memory gives.

For a variance decomposition the pieces are combined before the shares are
normalised and before groups are collapsed, so \code{normalise_gir} and
\code{max_groups} describe the whole chain rather than the piece they were
applied to.
}
\examples{

data("e1")
e1 <- diff(log(e1)) * 100
model <- create_bvarmodel(e1, p = 2, deterministic = "const",
                          iterations = 100, burnin = 10)
# Number of iterations and burn-in should be much higher.

model <- add_priors(model,
                    coef = list(v_i = 0, v_i_det = 0),
                    sigma = list(df = 1, scale = .0001))
model <- add_posterior_coefficients(add_initial_values(model))

file <- file.path(tempdir(), "bvartools-example-irf.h5")
unlink(file)
write_to_hdf5(model, filename = file)
stored <- open_model(file)

ir <- irf(stored, impulse = "invest", response = "income", n_ahead = 5,
          chunk = 25)
shares <- fevd(stored, response = "income", n_ahead = 5, chunk = 25)

}
\seealso{
Other model comparison:
\code{\link[=add_forecast_errors.bvarmodel]{add_forecast_errors.bvarmodel()}},
\code{\link[=add_forecast_errors.bvecmodel]{add_forecast_errors.bvecmodel()}},
\code{\link[=add_predictive_loglik]{add_predictive_loglik()}},
\code{\link[=add_predictive_loglik.bvarmodel]{add_predictive_loglik.bvarmodel()}},
\code{\link[=add_predictive_loglik.bvecmodel]{add_predictive_loglik.bvecmodel()}},
\code{\link[=align_model_obs.modellist]{align_model_obs.modellist()}},
\code{\link[=choose_best_model.selcritlist]{choose_best_model.selcritlist()}},
\code{\link[=create_external_forecast]{create_external_forecast()}},
\code{\link{folder_steps}},
\code{\link[=map_draws]{map_draws()}},
\code{\link[=map_models]{map_models()}},
\code{\link[=open_model]{open_model()}},
\code{\link[=open_models]{open_models()}},
\code{\link[=plot_forecast_errors_by_period]{plot_forecast_errors_by_period()}},
\code{\link[=selection_criteria.bvarmodel]{selection_criteria.bvarmodel()}},
\code{\link[=selection_criteria.bvecmodel]{selection_criteria.bvecmodel()}},
\code{\link[=selection_criteria.default]{selection_criteria.default()}},
\code{\link[=selection_criteria.modellist]{selection_criteria.modellist()}}
}
\concept{model comparison}
