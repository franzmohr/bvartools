% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{coint_prepare_sur_data}
\alias{coint_prepare_sur_data}
\title{Posterior Data Preparation}
\usage{
coint_prepare_sur_data(w, alpha, k, r, reparameterise, tvp)
}
\arguments{
\item{w}{a \eqn{T \times M} matrix.}

\item{alpha}{a \eqn{K \times r} matrix of the loading matrix \eqn{\alpha}.}

\item{k}{integer of the number of endogenous variables in the model.}

\item{r}{integer with the rank of the cointegration matrix \eqn{\Pi}.}

\item{reparameterise}{logical indicating if the reparameterisation described
in Koop et al. (2010) should be performed.}

\item{tvp}{logical indicating if the SUR matrix with the values of regressors
should be prepared for the estimation of constant or time varying parameters.}
}
\value{
A sparse block matrix.
}
\description{
Generates the input matrix for the simulation of the cointegration matrix \eqn{\beta}.
}
\examples{

data("e6") # Load data

# Generate model input
mod <- gen_vec(e6, p = 4, r = 1,
               const = "unrestricted", seasonal = "unrestricted")

# Obtain input data
alpha <- matrix(c(-0.1, 0.16, -0.04, -0.02), 2)
w <- mod$data$W

# Constant coefficients
coint_prepare_sur_data(w, alpha, 2, 1, TRUE, FALSE)

# Time varying coefficients
coint_prepare_sur_data(w, alpha, 2, 1, FALSE, TRUE)


}
