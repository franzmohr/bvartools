% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_regions.R
\name{create_regions}
\alias{create_regions}
\title{Create Region Series}
\usage{
create_regions(country.data, regions, period, region.weights, weight.data)
}
\arguments{
\item{country.data}{a named list of "zoo" objects containing country data.}

\item{regions}{a named list of character vectors containing the names of the countries, which belong to a region.
The name of the list element will be the name of the region.}

\item{period}{a character vector specifiying the periods in \code{region.weights}, which should be used to construct weights.}

\item{region.weights}{an object of class "zoo" containing the data used to weight the observations to construct regions.}

\item{weight.data}{a matrix or an array of weight data used to generate weight matrices in the GVAR model.}
}
\value{
A list containing the following components:
\item{country.data}{a named list containing the updated country models.}
\item{weight.data}{a matrix or an array containing the updated weights.}
}
\description{
Combines country-specific time series to regional series and updates the country and weight data.
}
\examples{
\dontrun{
data(gvar2016)

country.data <- gvar2016$country.data
weight.data <- gvar2016$weight.data
region.weights <- gvar2016$region.weights

period <- c("2008", "2009", "2010", "2011", "2012")
regions <- list("EuroArea" = c("Austria", "Belgium", "Finland", "France", "Germany", "Italy", "Netherlands", "Spain"))
regional.data <- create_regions(country.data, regions, period, region.weights, weight.data)

country.data <- regional.data$country.data
weight.data <- regional.data$weight.data
}

}
