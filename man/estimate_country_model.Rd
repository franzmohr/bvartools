% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_country_model.R
\name{estimate_country_model}
\alias{estimate_country_model}
\title{Estimate a Country Model}
\usage{
estimate_country_model(data, iterations = 50000, burnin = 5000, thin = 10)
}
\arguments{
\item{data}{a list containing the data, weights, specifications and priors of a country model, i.e. a component
of the list produced by \code{\link{country_models}}.}

\item{iterations}{an integer of MCMC draws including burn-in (defaults to 50000).}

\item{burnin}{an integer of MCMC draws used to initialize the sampler (defaults to 5000). These draws do
not enter the computation of posterior moments, forecasts etc.}

\item{thin}{an integer of thinning factor for MCMC output. Defaults to 10, which means that the forecast
sequences contain only every tenth draw of the original sequence. Set \code{thin = 1} to obtain the full MCMC sequence.}

\item{structural}{a logical specifying if the country models should be estimated as structual models a la Primicieri (2005).}

\item{tvp}{a logical specifying if the country models should be estimated with time-varying parameters
using the algorithm of Durbin & Koopman (2002).}

\item{sv}{a logical specifying if the country models should be estimated with stochastic volatility
using the algorithm of Kaster & Frühwirth-Schnatter (2014).}
}
\value{
A list of data, model specifications, priors, thinned coefficient draws and the values of information criteria.
}
\description{
Assigns the framework, i.e., data, specifications and priors, of a country model to a specified estimator function. The
function ensures that the estimation process is not stopped when an estimator function produces an error.
}
