% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/folder_methods.R
\name{folder_steps}
\alias{folder_steps}
\alias{add_priors.bvarfolder}
\alias{add_initial_values.bvarfolder}
\alias{add_seed.bvarfolder}
\alias{add_posterior_coefficients.bvarfolder}
\alias{add_posterior_loglik.bvarfolder}
\alias{thin.bvarfolder}
\alias{selection_criteria.bvarfolder}
\title{Estimation Steps on a Folder of Models}
\usage{
\method{add_priors}{bvarfolder}(object, ..., cores = 1)

\method{add_initial_values}{bvarfolder}(object, ..., cores = 1)

\method{add_seed}{bvarfolder}(object, seed, ..., cores = 1)

\method{add_posterior_coefficients}{bvarfolder}(object, ..., cores = 1, export = NULL)

\method{add_posterior_loglik}{bvarfolder}(object, ..., cores = 1)

\method{thin}{bvarfolder}(x, ..., cores = 1)

\method{selection_criteria}{bvarfolder}(object, ..., cores = 1)
}
\arguments{
\item{object}{an object of class 'bvarfolder', from
\code{\link{open_models}}.}

\item{...}{further arguments passed to the method for a single model.}

\item{cores}{the number of worker processes. Defaults to one.}

\item{seed}{an integer, the seed of the first model. The models are numbered
through in the order of the manifest, as the models of a list are.}

\item{export}{character vector of names of objects the workers need, for
instance the sampler that \code{posterior_function} refers to. See
\code{\link{map_models}}.}

\item{x}{an object of class 'bvarfolder'.}
}
\value{
The object in \code{object}, with its manifest brought up to date.
}
\description{
The steps of an estimation, applied to the models of a folder one at a time
rather than to a list of models in the session. Each reads a model, changes
it, writes it back and drops it, so a step costs one model per worker.
}
\details{
The methods do what their counterparts for a 'bvarmodel' or a 'bvecmodel'
do, model by model, and they take the same arguments.

Each model is drawn with a seed of its own, so the draws do not depend on how
many workers a step runs on, nor on whether the models were estimated in one
run or in several. The seeds are the ones a list of models gets from
\code{\link{add_seed}}: \code{seed}, \code{seed + 1}, and so on in the order
of the manifest.

A step takes \code{models}, which \code{\link{map_models}} understands: a
character vector of the models it is applied to, the rest of the folder being
left as it is. An estimation of many expensive models is run in parts that
way, and one that was interrupted is picked up where it stopped.

\code{add_posterior_coefficients} is the step to think twice about. It writes
the largest thing a model has, and a cluster of R workers carries every draw
into the session and out again to do it. Where the sampler is the BayesTS
executable, \code{\link{bayests_files}} runs it on the files instead:
\preformatted{run <- bayests_files(executable = "/opt/bayests/bin/bayests")
run(model_files(stored), jobs = 6)}
which gives the same draws, since a model is drawn with the seed in its file.

\code{selection_criteria} is the one method that reads rather than writes:
it hands back a criterion per model and leaves the folder as it is. It is
what \code{\link{choose_best_model}} compares, so a grid of specifications
too large to hold is still chosen from.
}
\examples{

data("e1")
train <- diff(log(e1)) * 100

models <- create_bvarmodel(data = train, p = 1:2, deterministic = "const",
                           iterations = 20, burnin = 10)

folder <- file.path(tempdir(), "bvartools-example-steps")
unlink(folder, recursive = TRUE)
dir.create(folder, recursive = TRUE)
write_to_hdf5(models, folder = folder)

stored <- open_models(folder)
stored <- add_priors(stored, coef = list(v_i = 1),
                     sigma = list(df = 3, scale = 1))
stored <- add_initial_values(stored)
stored <- add_seed(stored, 20260919)
stored <- add_posterior_coefficients(stored)
stored <- add_posterior_loglik(stored)

}
\seealso{
Other model comparison:
\code{\link[=add_forecast_errors.bvarmodel]{add_forecast_errors.bvarmodel()}},
\code{\link[=add_forecast_errors.bvecmodel]{add_forecast_errors.bvecmodel()}},
\code{\link[=add_predictive_loglik]{add_predictive_loglik()}},
\code{\link[=add_predictive_loglik.bvarmodel]{add_predictive_loglik.bvarmodel()}},
\code{\link[=add_predictive_loglik.bvecmodel]{add_predictive_loglik.bvecmodel()}},
\code{\link[=aggregate_forecasts]{aggregate_forecasts()}},
\code{\link[=align_model_obs.modellist]{align_model_obs.modellist()}},
\code{\link{analysis_of_stored_models}},
\code{\link[=choose_best_model.selcritlist]{choose_best_model.selcritlist()}},
\code{\link[=create_external_forecast]{create_external_forecast()}},
\code{\link[=map_draws]{map_draws()}},
\code{\link[=map_models]{map_models()}},
\code{\link[=open_model]{open_model()}},
\code{\link[=open_models]{open_models()}},
\code{\link[=plot_forecast_errors_by_period]{plot_forecast_errors_by_period()}},
\code{\link[=selection_criteria.bvarmodel]{selection_criteria.bvarmodel()}},
\code{\link[=selection_criteria.bvecmodel]{selection_criteria.bvecmodel()}},
\code{\link[=selection_criteria.default]{selection_criteria.default()}},
\code{\link[=selection_criteria.modellist]{selection_criteria.modellist()}}
}
\concept{model comparison}
