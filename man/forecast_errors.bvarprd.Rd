% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast_errors.bvarprd.R
\name{forecast_errors.bvarprd}
\alias{forecast_errors.bvarprd}
\title{Forecast Errors}
\usage{
\method{forecast_errors}{bvarprd}(object, test_sample, ...)
}
\arguments{
\item{object}{an object of class 'bvarprd', usually, the
result of a call to \code{\link{predict.bvarmodel}}.}

\item{test_sample}{a time-series object used as test data.}

\item{...}{arguments passed forward to method.}
}
\value{
A list of class 'fcsterror'.
}
\description{
Calculates forecast errors for models of class 'bvarprd'.
}
\examples{

# Load data
data("e1")
e1 <- diff(log(e1)) * 100
e1_long <- e1
e1 <- window(e1_long, end = c(1976, 4))

# Generate model data
model <- create_bvarmodel(e1, p = 0, deterministic = "const",
                          iterations = 100, burnin = 10)
# Chosen number of iterations and burnin should be much higher.

# Add prior specifications
model <- add_priors(model,
                    coef = list(v_i = 1, v_i_det = 1 / 10),
                    sigma = list(df = "k", scale = 1))

# Add initial values
model <- add_initial_values(model)

# Obtain posterior draws
object <- draw_posterior(model)

# Generate forecasts
bvar_pred <- predict(object, n_ahead = 10)

# Forecast errors
fe <- forecast_errors(bvar_pred, test_sample = e1_long)


}
