% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast_errors.expandwindbvarprdlist.R
\name{forecast_errors.expandwindbvarprdlist}
\alias{forecast_errors.expandwindbvarprdlist}
\title{Forecast Errors}
\usage{
\method{forecast_errors}{expandwindbvarprdlist}(object, test_sample, ...)
}
\arguments{
\item{object}{an object of class 'expandwindbvarprdlist', usually, the
result of a call to \code{\link{predict.expandwindbvarprdlist}}.}

\item{test_sample}{a time-series object used as test data.}

\item{...}{arguments passed forward to method.}
}
\value{
A list of class 'fcsterrorlist'.
}
\description{
Calculates forecast errors for a list of Bayesian models.
}
\examples{

data("us_macrodata")

# AR(1) models as benchmark
model <- create_bvarmodel(data = us_macrodata,
                          p = 1,
                          deterministic = "none",
                          error = "gamma",
                          iterations = 10,
                          burnin = 2)
# Chosen number of iterations and burn-in draws should be much higher.

# Obtain objects for expanding window estimation
model <- use_expanding_window(model, start = 2007)

# Add priors
model <- add_priors(model,
                    coef = list(v_i = 1, v_i_det = 1 / 10),
                    sigma = list(shape = 3, rate = 1))

# Add initial values
model <- add_initial_values(model)

# Obtain posterior draws
post <- draw_posterior(model)

# Predict
prd <- predict(post, n_ahead = 4)

# Forecast errors
fe <- forecast_errors(prd, test_sample = us_macrodata)


}
