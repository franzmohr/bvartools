% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.bvarmodel.R
\name{predict.bvarmodel}
\alias{predict.bvarmodel}
\title{Predict Method for Objects of Class bvar}
\usage{
\method{predict}{bvarmodel}(object, n_ahead = NULL, ...)
}
\arguments{
\item{object}{an object of class 'bvarmodel'.}

\item{n_ahead}{number of steps ahead at which to predict. If \code{NULL} (default), every
period simulated by \code{\link{add_posterior_forecasts}}, i.e. the horizon given to
\code{\link{add_forecast_input}}.}

\item{...}{additional arguments.}
}
\value{
An object of class 'bvarprd', a list with element \code{fcst}, an
\eqn{h \times K \times S} array of the \eqn{S} forecast draws, whose first two
dimensions are named after the forecast periods and the endogenous variables, and
element \code{y}, the endogenous variables of the training sample.
}
\description{
Forecasting a Bayesian VAR object of class 'bvarmodel'.
}
\details{
For the VAR model
\deqn{A_0 y_t = \sum_{i = 1}^{p} A_{i} y_{t-i} + \sum_{i = 0}^{s} B_{i} x_{t-i} + C D_t + u_t,}
with \eqn{u_t \sim N(0, \Sigma)} the function produces \code{n_ahead} forecasts.
}
\examples{

# Load data
data("e1")
e1 <- diff(log(e1)) * 100
e1 <- window(e1, end = c(1978, 4))

# Generate model data
model <- create_bvarmodel(e1, p = 2, deterministic = "const",
                          iterations = 100, burnin = 10)
# Chosen number of iterations and burnin should be much higher.

# Add prior specifications
model <- add_priors(model,
                    coef = list(v_i = 1, v_i_det = 1 / 10),
                    sigma = list(df = "k", scale = 1))

# Add initial values
model <- add_initial_values(model)

# Obtain posterior draws
model <- add_posterior_coefficients(model)

# Add the data the forecasts are produced from
model <- add_forecast_input(model, n_ahead = 10)

# Simulate the forecasts
model <- add_posterior_forecasts(model)

# Collect them
pred <- predict(model)

}
\references{
Lütkepohl, H. (2006). \emph{New introduction to multiple time series analysis} (2nd ed.). Berlin: Springer.
}
\seealso{
Other post-estimation analysis:
\code{\link[=add_sign_restrictions.bvarmodel]{add_sign_restrictions.bvarmodel()}},
\code{\link[=add_sign_zero_restrictions.bvarmodel]{add_sign_zero_restrictions.bvarmodel()}},
\code{\link[=fevd.bvarmodel]{fevd.bvarmodel()}},
\code{\link[=fevd.bvecmodel]{fevd.bvecmodel()}},
\code{\link[=irf.bvarmodel]{irf.bvarmodel()}},
\code{\link[=irf.bvecmodel]{irf.bvecmodel()}},
\code{\link[=multipliers]{multipliers()}},
\code{\link[=multipliers.bvarmodel]{multipliers.bvarmodel()}},
\code{\link[=multipliers.bvecmodel]{multipliers.bvecmodel()}},
\code{\link[=spillover.bvarmodel]{spillover.bvarmodel()}},
\code{\link[=spillover.bvecmodel]{spillover.bvecmodel()}},
\code{\link[=vec_to_var.bvecmodel]{vec_to_var.bvecmodel()}}
}
\concept{post-estimation analysis}
