% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.posteriorlist.R
\name{predict.posteriorlist}
\alias{predict.posteriorlist}
\title{Predict Method for Objects of Class posteriorlist}
\usage{
\method{predict}{posteriorlist}(object, ...)
}
\arguments{
\item{object}{an object of class 'posteriorlist'.}

\item{...}{additional arguments.}
}
\value{
A time-series object of class 'bvarprdlist'.
}
\description{
Forecasting Bayesian VAR objects in a list of class 'posteriorlist'.
}
\examples{

# Load data
data("e1")
e1 <- diff(log(e1)) * 100
e1 <- window(e1, end = c(1978, 4))

# Generate model data
model <- create_bvarmodel(e1, p = 1:2, deterministic = "const",
                          iterations = 100, burnin = 10)
# Chosen number of iterations and burnin should be much higher.

# Add prior specifications
model <- add_priors(model,
                    coef = list(v_i = 1, v_i_det = 1 / 10),
                    sigma = list(df = "k", scale = 1))

# Add initial values
model <- add_initial_values(model)

# Obtain posterior draws
object <- draw_posterior(model)

# Generate forecasts
bvar_pred <- predict(object, n_ahead = 10)


}
