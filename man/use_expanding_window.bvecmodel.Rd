% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_expanding_window.bvecmodel.R
\name{use_expanding_window.bvecmodel}
\alias{use_expanding_window.bvecmodel}
\title{Expanding Window Estimation}
\usage{
\method{use_expanding_window}{bvecmodel}(object, start, ...)
}
\arguments{
\item{object}{an object of class 'bvecmodel' containing model specifications
and input data. Usually, the output of a call to  \code{\link{create_bvecmodel}}.}

\item{start}{the start period of the prediction of the first iteration of the
expanding window approach.}

\item{...}{arguments passed forward to method.}
}
\value{
A list of class 'expandingwindow' with one object of class 'bvecmodel' per
window. The training sample of the first ends in the period before \code{start}, and
each further window adds one period. Posterior draws, starting values and forecast input
that \code{object} already carries belong to the whole sample and are not copied into
the windows; a warning says so.
}
\description{
Creates objects for expanding window posterior simulation.
}
\examples{

# Load data 
data("e6")
e6 <- e6 * 100

# Generate model
model <- create_bvecmodel(e6, p = 1, r = 1, const = "restricted",
                          iterations = 10, burnin = 10)
# Chosen number of iterations and burn-in should be much higher.

# Create multiple model objects for expanding window
model <- use_expanding_window(model, start = 1998)

}
\seealso{
Other model set-up:
\code{\link[=add_initial_values.bvarmodel]{add_initial_values.bvarmodel()}},
\code{\link[=add_initial_values.bvecmodel]{add_initial_values.bvecmodel()}},
\code{\link[=add_priors.bvarmodel]{add_priors.bvarmodel()}},
\code{\link[=add_priors.bvecmodel]{add_priors.bvecmodel()}},
\code{\link[=combine_models]{combine_models()}},
\code{\link[=create_bvarmodel]{create_bvarmodel()}},
\code{\link[=create_bvecmodel]{create_bvecmodel()}},
\code{\link[=transform_variables]{transform_variables()}},
\code{\link[=use_expanding_window.bvarmodel]{use_expanding_window.bvarmodel()}}
}
\concept{model set-up}
