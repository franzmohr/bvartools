% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_to_hdf5.bvarmodel.R
\name{write_to_hdf5.bvarmodel}
\alias{write_to_hdf5.bvarmodel}
\title{Export to HDF5 File}
\usage{
\method{write_to_hdf5}{bvarmodel}(object, filename, ...)
}
\arguments{
\item{object}{list of class 'bvarmodel'.}

\item{filename}{path to the file, in which output should be stored.}

\item{...}{further arguments passed to or from other methods.}
}
\description{
Exports the content of an object of class 'bvarmodel' to an HDF5 file.
}
\examples{

# Load data
data("e1")
train <- diff(log(e1)) * 100

# Create model
model <- create_bvarmodel(data = train,
                          p = 1,
                          deterministic = "const",
                          iterations = 10,
                          burnin = 10)
# Number of iterations and burnin should be much higher.

# Add priors
model <- add_priors(model,
                    coef = list(v_i = 1),
                    sigma = list(df = 3, scale = 1))

# Add initial values
model <- add_initial_values(model)

# Save model
path_to_model <- tempfile(fileext = ".h5")
write_to_hdf5(model, filename = path_to_model)

}
