% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_to_hdf5.modellist.R
\name{write_to_hdf5.modellist}
\alias{write_to_hdf5.modellist}
\title{Export to HDF5 File}
\usage{
\method{write_to_hdf5}{modellist}(object, folder, ...)
}
\arguments{
\item{object}{an object of class 'modellist'.}

\item{folder}{path to the folder where the individual models in argument
\code{object} should be saved.}

\item{...}{further arguments passed to or from other methods.}
}
\description{
Exports the content of an object of class 'modellist' to multiple HDF5 file.
}
\examples{

# Load data
data("e1")
train <- diff(log(e1)) * 100

# Create model
model <- create_bvarmodel(data = train,
                          p = 0:4,
                          deterministic = "const",
                          iterations = 10,
                          burnin = 10)
# Number of iterations and burnin should be much higher.

# Add priors
model <- add_priors(model,
                    coef = list(v_i = 1),
                    sigma = list(df = 3, scale = 1))

# Add initial values
model <- add_initial_values(model)

# Save model
path_to_target_directory <- tempdir()
write_to_hdf5(model, folder = path_to_target_directory)


}
