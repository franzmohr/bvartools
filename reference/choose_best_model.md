# Choose Best Model

A generic function that applies model selection criteria to find the
best model.

## Usage

``` r
choose_best_model(object, ...)
```

## Arguments

- object:

  an object with suitable input data passed forward to method.

- ...:

  arguments passed forward to method.

## Value

The value returned by the method for the class of `object`, as described
on the pages of the methods.

## See also

Methods:
[`choose_best_model.selcritlist`](https://franzmohr.github.io/bvartools/reference/choose_best_model.selcritlist.md).
